# Open questions and hypotheses

These are benchmarkable hypotheses, not locked commitments: Postgres JSONB versus normalized tables for early document structure; pgvector versus a separate vector service; graph projection shape; extraction model selection; ER candidate generation and thresholds; OTel/Logfire deployment; and UI framework. Decisions require representative synthetic benchmarks, cost/latency measurements, and evidence-quality review.

