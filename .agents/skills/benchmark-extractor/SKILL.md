---
name: benchmark-extractor
description: Benchmark document structuring and semantic extraction with reproducible manifests, layer-specific metrics, evidence, cost, and latency. Use for OCR, table reconstruction, schema mapping, extractor, prompt, model, or parser quality changes.
---

# Benchmark extractor

Create a reproducible manifest with synthetic/public inputs, schema/version, expected locations, metrics, tolerances, model/prompt versions, cost, and latency. Report failures by layer; do not hide missing evidence behind aggregate accuracy.
