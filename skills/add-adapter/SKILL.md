# Add adapter

Read AGENTS and relevant ADRs. Define a port first, implement mechanics behind it, add contract tests, wire only in the composition root, and document evidence/operational failure modes.

